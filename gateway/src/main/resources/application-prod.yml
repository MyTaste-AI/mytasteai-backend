spring:
  config:
    activate:
      on-profile: prod

logging:
  level:
    root: INFO
    myaimentor_api: INFO

jwt:
  secret: ${JWT_SECRET}

routes:
  user-service: ${USER_SERVICE_URL}
  mentor-service: ${MENTOR_SERVICE_URL}
  chat-service: ${CHAT_SERVICE_URL}
